id: positive-trigger-004
name: Positive Trigger 4 - Implement Verb
description: >-
  Tuning-derived case: realistic, non-echoing coding request using the
  "implement" verb, no git/GitHub tokens, attributable to the broadened
  description wording.
tags:
  - positive-trigger
  - tuning-derived
inputs:
  prompt: "Implement a caching layer for the user profile endpoint."
expected:
  should_trigger: true
graders:
  - type: trigger
    name: positive-trigger
    config:
      skill_path: skills/dev-platform-workflow/SKILL.md
      mode: positive
