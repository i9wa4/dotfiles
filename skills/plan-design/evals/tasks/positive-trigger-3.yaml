id: positive-trigger-003
name: Positive Trigger 3
description: Curated trigger-accuracy prompt for human-facing artifact handoffs.
tags:
  - positive-trigger
inputs:
  prompt: "Use plan-design to prepare a human-facing Secret-Gist handoff from an mkmd task artifact, follow the active runtime language policy, verify the Gist safely, and report DONE/BLOCKED evidence through Postman."
expected:
  should_trigger: true
graders:
  - type: trigger
    name: positive-trigger
    config:
      skill_path: skills/plan-design/SKILL.md
      mode: positive
