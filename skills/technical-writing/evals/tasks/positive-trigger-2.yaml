id: positive-trigger-002
name: Positive Trigger 2
description: Curated from the repo trigger matrix (skills/trigger-validation.json).
tags:
  - positive-trigger
inputs:
  prompt: "Use Vale and Harper to check English README prose, grammar, spelling, terminology, and false positives."
expected:
  should_trigger: true
graders:
  - type: trigger
    name: positive-trigger
    config:
      skill_path: skills/technical-writing/SKILL.md
      mode: positive
