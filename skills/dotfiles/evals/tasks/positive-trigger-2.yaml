id: positive-trigger-002
name: Positive Trigger 2
description: Curated trigger-accuracy prompt for this skill.
tags:
  - positive-trigger
inputs:
  prompt: "Set up a new machine, run nix run .#switch, resolve a missing command, or fix clipboard behavior across Vim, Neovim, and tmux."
expected:
  should_trigger: true
graders:
  - type: trigger
    name: positive-trigger
    config:
      skill_path: skills/dotfiles/SKILL.md
      mode: positive
