id: positive-trigger-002
name: Positive Trigger 2 - Add Verb
description: >-
  Tuning-derived case: realistic, non-echoing coding request using the "add"
  verb, no git/GitHub tokens, attributable to the broadened description
  wording.
tags:
  - positive-trigger
  - tuning-derived
inputs:
  prompt: "Add a retry with exponential backoff to the S3 upload handler."
expected:
  should_trigger: true
graders:
  - type: trigger
    name: positive-trigger
    config:
      skill_path: skills/dev-platform-workflow/SKILL.md
      mode: positive
