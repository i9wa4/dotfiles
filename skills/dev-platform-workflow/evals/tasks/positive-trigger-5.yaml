id: positive-trigger-005
name: Positive Trigger 5 - Independent Cache Invalidation
description: >-
  Independent, non-tuning-derived regression case for a realistic code-change
  request outside the exact prompts used during tuning.
tags:
  - positive-trigger
  - independent-regression
inputs:
  prompt: "Implement cache invalidation to prevent duplicate session refreshes."
expected:
  should_trigger: true
graders:
  - type: trigger
    name: positive-trigger
    config:
      skill_path: skills/dev-platform-workflow/SKILL.md
      mode: positive
