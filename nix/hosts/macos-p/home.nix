# Host-specific home-manager settings for macos-p (personal)
_: {
  imports = [
    ../../common/home.nix
  ];

  # Host-specific home-manager settings can be added here
  # Example: additional packages, environment variables, etc.
}
