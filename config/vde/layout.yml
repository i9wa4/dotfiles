presets:
  dev:
    name: dev
    description: dev
    windowMode: current-window
    layout:
      type: horizontal
      ratio: [1, 1]
      panes:
        - name: editor
          command: vim -c "execute 'edit' '.i9wa4/temp.md'->expand()"
          focus: true
        - name: claude
          command: A2A_PEER=worker-w claude --dangerously-skip-permissions

  orchestrator:
    name: orchestrator
    description: orchestrator
    windowMode: current-window
    layout:
      type: vertical
      ratio: [8, 1]
      panes:
        - type: horizontal
          ratio: [1, 1]
          panes:
            - name: editor
              command: vim -c "execute 'edit' '.i9wa4/temp.md'->expand()"
              focus: true
            - name: orchestrator
              command: A2A_PEER=orchestrator claude --dangerously-skip-permissions
        - name: postman
          command: uv run ~/ghq/github.com/i9wa4/dotfiles/bin/postman.py

  workers:
    name: workers
    description: workers
    windowMode: new-window
    layout:
      type: vertical
      ratio: [1, 1]
      panes:
        - type: horizontal
          ratio: [1, 1]
          panes:
            - name: worker
              command: A2A_PEER=worker-r codex --yolo
            - name: worker
              command: A2A_PEER=worker-w claude --dangerously-skip-permissions
        - type: horizontal
          ratio: [1, 1, 1, 1]
          panes:
            - name: observer
              command: A2A_PEER=observer-codex-1 codex --yolo
            - name: observer
              command: A2A_PEER=observer-codex-2 codex --yolo
            - name: observer
              command: A2A_PEER=observer-claude-1 claude --dangerously-skip-permissions
            - name: observer
              command: A2A_PEER=observer-claude-2 claude --dangerously-skip-permissions
