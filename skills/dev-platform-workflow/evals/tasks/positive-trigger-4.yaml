id: positive-trigger-004
name: Positive Trigger 4 - Implement Verb
description: >-
  Realistic, non-echoing coding request using the "implement" verb, no
  git/GitHub tokens, independently attributable to the broadened description
  wording.
tags:
  - positive-trigger
inputs:
  prompt: "Implement a caching layer for the user profile endpoint."
expected:
  should_trigger: true
graders:
  - type: trigger
    name: positive-trigger
    config:
      skill_path: skills/dev-platform-workflow/SKILL.md
      mode: positive
