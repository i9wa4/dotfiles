#!/usr/bin/env bash
set -o errexit
set -o nounset
set -o pipefail
set -o posix

# .devcontainerディレクトリをコピー（存在する場合はスキップ）
# if [ ! -d ".devcontainer" ] && [ -d "$HOME/ghq/github.com/i9wa4/dotfiles/.devcontainer" ]; then
#   rm -rf .devcontainer/
#   cp -r "$HOME/ghq/github.com/i9wa4/dotfiles/.devcontainer" .
#   echo "✓  .devcontainer ディレクトリをコピーしました"
# fi

# cp -f "${HOME}"/ghq/github.com/i9wa4/dotfiles/dot.config/claude/dot.mcp.json .mcp.json
# echo "✓  .mcp.json ファイルをコピーしました"

cat >local.vim <<'EOF'
" ~/ghq/github.com/i9wa4/dotfiles/dot.config/vim/rc/local.default.vim
let g:mnh_header_level_shift = 1
let g:my_tp_path = '.i9wa4/temp.md'->expand()
EOF
echo "✓  local.vim ファイルをコピーしました"

# .i9wa4/ ディレクトリとファイルを作成
mkdir -p .i9wa4/
echo "✓  .i9wa4/ ディレクトリを作成しました"

cat >.i9wa4/temp.md <<'EOF'
# temp

## Common

/CONTRIBUTING

## Claude Code

## Codex CLI

## Review

/CONTRIBUTING
/my-review
/summarize-reviews
EOF

# mise.toml が存在する場合は trust する
if [ -f "mise.toml" ]; then
  mise trust mise.toml
  echo "✓  mise.toml を trust しました"
fi

echo "✓  セットアップ完了"
