id: positive-trigger-004
name: Positive Trigger 4
description: Curated trigger for canonical versus multiple artifact decisions.
tags:
  - positive-trigger
inputs:
  prompt: "For local markdown artifacts, decide canonical-vs-multiple handling: two task trackers claim the same issue while a research digest has a distinct purpose; reuse one tracker and keep the separate-purpose record."
expected:
  should_trigger: true
graders:
  - type: trigger
    name: positive-trigger
    config:
      skill_path: skills/logbook/SKILL.md
      mode: positive
