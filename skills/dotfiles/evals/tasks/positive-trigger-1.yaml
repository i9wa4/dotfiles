id: positive-trigger-001
name: Positive Trigger 1
description: Curated from the repo trigger matrix (skills/trigger-validation.json).
tags:
  - positive-trigger
inputs:
  prompt: "Update the repository agent harness, orchestrator role/runbooks, issue worktree workflow, or postman routing rules."
expected:
  should_trigger: true
graders:
  - type: trigger
    name: positive-trigger
    config:
      skill_path: skills/dotfiles/SKILL.md
      mode: positive
